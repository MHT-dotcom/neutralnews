# Testing dependencies for Neutral News
# Install with: pip install -r requirements_test.txt

# Include all production dependencies
-r requirements.txt

# Testing specific dependencies
pytest==7.4.0
pytest-cov==4.1.0
pytest-mock==3.11.1
# Remove incompatible pytest-flask
# pytest-flask==1.2.0  # This version is incompatible with newer Flask
responses==0.23.3  # For mocking HTTP responses
coverage==7.3.2    # For code coverage reporting

# Pin Flask to a compatible version if needed
# flask==2.0.3  # Uncomment if you want to pin Flask to a specific version 